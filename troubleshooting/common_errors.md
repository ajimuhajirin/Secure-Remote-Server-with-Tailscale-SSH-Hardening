# 🚑 Common Errors

## ❌ SSH Connection Refused

Solusi:

```bash
sudo systemctl status ssh
```

---

## ❌ Permission Denied

Solusi:

* cek SSH key
* cek config ssh

---

## ❌ Tailscale tidak connect

Solusi:

```bash
sudo tailscale up
```
